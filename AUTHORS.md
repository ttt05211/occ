# Authors and source attribution

This standalone extraction is derived from the project history in `ttt05211/swfm`. The original Git history remains the authoritative record for individual authorship and contribution provenance; it is intentionally not rewritten or replaced here.

No source file in the inspected frozen repository carried a top-level project license statement that would justify replacing its attribution/license terms with a new blanket license. See `LICENSE_STATUS.md`.

The extraction/refactor keeps exact source commit mappings in `docs/PROVENANCE.md` so downstream users can trace each frozen method/runtime contract back to the original repository history.
